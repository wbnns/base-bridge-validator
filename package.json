{
  "name": "base-bridge-validator",
  "version": "0.1.0",
  "description": "Validate the Base Bridge Address on the Superchain Token List for new token additions on bridge.base.org.",
  "scripts": {
    "build": "tsc",
    "validate": "node dist/validateBaseBridgeAddress.js"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "typescript": "4.9.4",
    "@types/node": "20.11.1",
    "@types/axios": "^0.14.0"
  }
}
